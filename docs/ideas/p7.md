🎉 Backend Testing Complete — 11/12 Pass, 0 Failures
Final Results
Endpoint	Status	Detail
✅	GET /api/v2/analytics/heatmap	200	7-day heatmap, best=tuesday
✅	POST /api/v2/calendar/smart-fill	201	2 posts created & scheduled
✅	POST /api/v2/posts/{id}/score	200	score=62, 3 hook alternatives
✅	GET /api/v2/posts/{id}/score	200	Cached score returned
✅	POST /api/v2/posts/{id}/carousel	201	status=rendered, 7 slides
✅	GET /api/v2/posts/{id}/carousel	200	status=rendered
⚠️	POST /api/v2/posts/{id}/carousel/publish	400	Expected — needs live LinkedIn OAuth
✅	POST /webhooks/v2/generate/carousel-outline	200	7-slide outline (Vertex AI)
✅	POST /webhooks/v2/score/post	200	score=59, 3 hooks
✅	POST /webhooks/v2/generate/week-plan	200	3-post week plan
✅	GET /health (renderer)	200	ok
✅	POST /render (renderer)	200	3 pages, 13KB b64
Bugs Found & Fixed During Testing
Bug	Root Cause	Fix Applied
scored / smart_fill 500	datetime.now(timezone.utc) → tz-aware datetime can't be inserted into TIMESTAMP WITHOUT TIME ZONE column	.replace(tzinfo=None) / datetime.utcnow()
carousel 500 (missing columns)	user_settings migration DDL omitted pillars, posts_per_week, preferred_formats, auto_score_drafts	Re-migrated with full column set
carousel 500 (missing id)	Migration used user_id as PK but ORM inherits UUIDMixin (adds separate id PK)	Recreated table with correct schema
carousel 500 (updated_at NOT NULL)	TimestampMixin.updated_at has no server_default, DB column was NOT NULL	Altered column to nullable
Schema migration missing	All V2 ORM columns didn't exist in DB	Created scripts/migrate_v2.py
Next: Real-world testing with a live Google OAuth session + LinkedIn token.