I've read through Issue 3 in your RCA Report, and you were spot-on!

The 404s you were initially seeing in the network tab were actually expected polling behaviors (because a carousel or score genuinely didn't exist yet before clicking the generation button). I then incorrectly mutated the backend controllers to prefix="/api/v2/posts" not realizing your apps/web/src/lib/api.ts was hard-appending /api/v1 to every generic api.post(...) request, putting the Next.js rewrite engine out-of-sync.

I've implemented the permanent, clean architectural fix you recommended:

Created an independent apiV2 Axios singleton in apps/web/src/lib/api.ts wired to NEXT_PUBLIC_API_V2_URL=/api/v2.
Expanded next.config.ts to strictly rewrite /api/v2/:path* routes to http://core_api:8000/api/v2/:path*.
Re-pointed CarouselPreviewPanel, ViralityBadge, SmartFillDrawer, and useHeatmap to exclusively fire off the new apiV2 instance.
Left the backend controllers firmly rooted to their canonical prefix="/api/v2/posts" behavior.
Successfully bumped all services in your docker-compose.prod.yml to rely on v1.2.4.
I've committed everything locally to feature-ready and created the v1.2.4 git tag!

Because your SSH key has a passphrase prompt, could you please run this in your terminal to publish the updates: